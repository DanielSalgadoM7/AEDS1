/*#include <stdio.h>

int main() {
  int opcao = 0, contAlcool = 0, contGasolina = 0, contDiesel = 0;
  scanf("%d", &opcao);
  do {
    switch (opcao) {
    case 1:
      contAlcool++;
      contGasolina = 0;
      contDiesel = 0;
      printf("Alcool: %d\n", contAlcool);

    case 2:
      contGasolina++;
      opcao = 0;
      printf("Gasolina: %d\n", contGasolina);

    case 3:
      contDiesel++;
      opcao = 0;
      printf("Diesel: %d\n", contDiesel);
    case 4:
      break;

      default:
      scanf("%d", &opcao);
    }
  } while (opcao != 4);

  printf("MUITO OBRIGADO\n");
  printf("Alcool: %d\n", contAlcool);
  printf("Gasolina: %d\n", contGasolina);
  printf("Diesel: %d\n", contDiesel);

  return 0;
}
*/
